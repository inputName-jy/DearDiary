
</div>

    <div>
        <h2>Posts</h2>
        {{#each posts as |post|}}
        <div class="cards-containerj">
            <div class="cardj">
                <a href="/post/{{post.id}}">
                <h2>{{post.title}}</h2>
                <p>Posted by: {{post.user.username}} on {{format_date post.createdAt}}</p>
                </a>
            </div>
        </div>
        {{/each}}

    </div>
</div>
